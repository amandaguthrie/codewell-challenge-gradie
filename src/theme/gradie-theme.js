import { extendTheme } from '@chakra-ui/react';

const gradieTheme = extendTheme({
  fonts: {
    heading: `'Roboto', sans-serif`,
    body: `'Roboto', sans-serif`,
  },
});

export default gradieTheme;